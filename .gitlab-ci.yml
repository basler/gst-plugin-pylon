# GitHub Actions is the CI source of truth for gst-plugin-pylon
# (pylon 26.06 via Conan, Ubuntu 24.04 / Windows / native arm64).
# This GitLab pipeline is disabled so it does not keep testing retired
# Ubuntu 20.04 images and pylon 6.3/7.1 tarballs.
workflow:
  rules:
    - when: never

stages:
  - fetch
  - test
