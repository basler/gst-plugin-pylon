if not get_option('tests').disabled() and gstcheck_dep.found()
  subdir('check')
endif

if not get_option('examples').disabled()
  subdir('examples')
endif

if not get_option('prototypes').disabled()
  subdir('prototypes')
endif
